# 工具调用流程与执行顺序：v2 串行，并行列后续

工具调用全流程：CallerPolicy 过滤工具 schema → 流式解析（容灾第 1 层）→ 调度守卫与懒注册 → 参数校验 → handler 执行（超时与 abort）→ 结构化结果回填 → 审计。同一轮模型输出中的多个 tool_call 在 v2 按输出顺序串行执行，避免并发写冲突并简化取消语义。并行执行列入后续优化：届时先做拓扑排序，仅并行互不影响、或不依赖前序结果的调用。
