# 用户插件编译期内置，v2 不提供动态加载与插件市场

v2 的用户插件与内核同处一个 Rust crate（src/kernel 与 src/plugin 目录分区），kernel 通过静态插件清单加载；不实现运行时动态加载（DLL/WASM）、也不提供"创意工坊"式的第三方插件分发。原因：插件目前只由本项目开发者编写，动态加载带来的 ABI 稳定性、加载安全、Windows 打包和分发成本没有收益。若将来出现第三方插件需求，在编译期内置之外加一层动态加载适配器即可，内核契约保持不变。
